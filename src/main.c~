/*
** EPITECH PROJECT, 2026
** main
** File description:
** main
*/

#include <ncurses.h>

#include "my.h"

WINDOW *ncurse_init

WINDOW *screen = initscr();

    noecho();
    nodelay(screen, true);
    if (has_colors() == true) {
        start_color();
        use_default_colors();

int main(void)
{
    my_putstr("OK\n");
    return 0;
}
